package abc.abc464

fun main() {
    val t = readln().toInt()
    repeat(t) {
        val n = readln().toInt()
        val s = readln()
        val x = readln().split(" ").map{it.toInt()}
        val y = readln().split(" ").map{it.toInt()}
        
    }
}